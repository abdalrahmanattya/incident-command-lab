global: {scrape_interval: 15s}
scrape_configs:
  - job_name: incidentlab
    static_configs: [{targets: [gateway:8080]}]
  - job_name: otel-collector
    static_configs: [{targets: [otel-collector:8889]}]
rule_files: [/etc/prometheus/rules.yml]
